[server]
port = 8502
address = "0.0.0.0"
headless = true

[browser]
gatherUsageStats = false

[runner]
# Disable automatic multi-page detection from the pages/ folder
# All routing is handled by app.py
magicEnabled = false
